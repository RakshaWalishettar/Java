package com.xworkz.Inheritance.external.Runner;

import com.xworks.Inheritance.external.GeneralManager;
import com.xworks.Inheritance.external.Projectmanager;

public class ProjectRunner {
    public static void main(String[] args){
        GeneralManager gm=new GeneralManager();
        GeneralManager general=new Projectmanager();
    }
}
